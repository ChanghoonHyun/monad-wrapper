{
  "name": "monad-wrapper",
  "version": "0.0.1",
  "private": true,
  "description": "Wrap by monad",
  "keywords": [],
  "author": "",
  "license": "",
  "main": "index.js",
  "dependencies": {
    "axios": "^0.18.0",
    "monet": "^0.8.10",
    "ramda": "^0.25.0"
  },
  "devDependencies": {
    "mocha": "^5.0.1",
    "should": "^7.1.0"
  },
  "scripts": {
    "test": "node_modules/mocha/bin/mocha $(find ./test/ -name '**/*.js') --recursive --timeout 5000 --exit"
  }
}
